import React from 'react';
import { withRouter } from 'react-router';

const PostsHeader = () => {
    return (
        <div>
            Posts Header
        </div>
    )
}

export default withRouter(PostsHeader);