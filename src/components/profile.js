import React from "react";
import Card from './hoc/card';

const Profile = (props) => {
  console.log(props);
  return (
    <>
      <Card>
        Hello Guys
      </Card>
    </>
  );
};

export default Profile;
